package com.kaboomroads.lostfeatures.utils;

import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;

public record SculkPlacement(BlockPos pos, BlockState state) {
}
